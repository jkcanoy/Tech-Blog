<div class="card p-2 mt-3">
    <div class="card-header">
        <h5><a class="link" href="/post/{{id}}">{{title}}</a></h5>
        <p>Posted by {{user.username}} on {{format_date created_at}}</p>
    </div>
    <div class="card-body">
        <p>{{content}}</p>
    </div>
</div>